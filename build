#!/usr/bin/env bash
# The page is embedded in the dagger binary, so it's built first when its tooling is there.
set -euo pipefail
cd "$(dirname "$0")"
if [ -d ui/node_modules ]; then
  npm --prefix ui run build
fi
cargo build --workspace "$@"
